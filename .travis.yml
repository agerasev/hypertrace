language: rust

os: linux
dist: bionic

rust: nightly

addons:
  apt:
    packages:
      - clang
      - cmake
      - make

before_install:
  - sudo apt-get update
install:
  - sudo apt-get install -y googletest ocl-icd-opencl-dev pocl-opencl-icd libpocl-dev

script:
  - cargo test
